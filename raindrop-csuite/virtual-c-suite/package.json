{
  "name": "virtual-c-suite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LiquidMetal AI app",
  "scripts": {
    "build": "shx rm -rf dist && tsc -b",
    "start": "raindrop build deploy --start",
    "stop": "raindrop build stop",
    "restart": "raindrop build deploy --start",
    "dev:upload": "cross-env PORT=3000 tsx watch src/upload-api/index.ts",
    "dev:coord": "cross-env PORT=3001 tsx watch src/analysis-coordinator/index.ts",
    "dev:processor": "cross-env PORT=3002 tsx watch src/board-meeting-processor/index.ts",
    "dev:all": "run-p dev:upload dev:coord dev:processor",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hono/node-server": "^1",
    "@liquidmetal-ai/raindrop-framework": "0.12.0",
    "@modelcontextprotocol/sdk": "^1",
    "dotenv": "^17.2.3",
    "hono": "^4",
    "kysely": "^0.27.2",
    "kysely-d1": "^0.3.0",
    "posthog-node": "^5.17.2",
    "zod": "^3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "cross-env": "^10.1.0",
    "npm-run-all": "^4.1.5",
    "shx": "^0.4.0",
    "ts-node": "^10",
    "tsx": "^4",
    "typescript": "^5.0.4",
    "typescript-eslint": "^8.7.0",
    "vitest": "^3.1.3"
  }
}
